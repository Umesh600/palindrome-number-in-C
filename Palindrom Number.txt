#include <stdio.h>

int main()
{
    
    int n,r,temp,sum=0;
    
    printf("Enter any number: ");
    scanf("%d",&n);
    
    temp=n;
    
    while(n>0)
    {
        r=n%10;
        sum=(sum*10)+r;
        n=n/10;
        
    }
    
    if(temp==sum)
    {
        printf("Number is palindrom");
        
    }
    else 
    {
        printf("Number is not palindrom");
    }
  
    return 0;
}


output=  Number is palindrom or not
